[General]
arch=mipsel
directory=/opt/multistrap/
# same as --tidy-up option if set to true
cleanup=true
# same as --no-auth option if set to true
# keyring packages listed in each bootstrap will
# still be installed.
noauth=true
# extract all downloaded archives (default is true)
unpack=true
# whether to add the /suite to be explicit about where apt
# needs to look for packages. Default is false.
explicitsuite=false
# aptsources is a list of sections to be used
# the /etc/apt/sources.list.d/multistrap.sources.list
# of the target. Order is not important
aptsources=Debian
# the bootstrap option determines which repository
# is used to calculate the list of Priority: required packages
# and which packages go into the rootfs.
# The order of sections is not important.
bootstrap=Debian

[Debian]
packages=busybox-static procps amule-daemon dtach git less rtorrent tesseract-ocr apt lighttpd curl vim xmlstarlet openssh-client irssi mercurial python-pycurl python-imaging python-django spidermonkey-bin libwww-curl-perl liburi-perl libgd-gd2-perl sudo lftp tcpdump locales
source=http://ftp.tw.debian.org/debian
keyring=debian-archive-keyring
suite=testing
omitdebsrc=true
